<?xml version="1.0" encoding="utf-8"?>
<com.facebook.shimmer.ShimmerFrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/shimmer_view_container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:shimmer_auto_start="true"
    app:shimmer_base_alpha="0.8"
    app:shimmer_highlight_alpha="0.5"
    app:shimmer_base_color="@color/skeleton_bg"
    app:shimmer_highlight_color="@color/shimmer_color">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <!-- Header -->
        <View android:layout_width="180dp" android:layout_height="36dp" android:background="@drawable/skeleton_placeholder" android:layout_marginBottom="24dp" />

        <!-- BMI Card -->
        <View android:layout_width="match_parent" android:layout_height="250dp" android:background="@drawable/skeleton_placeholder" android:layout_marginBottom="16dp" />

        <!-- Calories Cards -->
        <View android:layout_width="match_parent" android:layout_height="120dp" android:background="@drawable/skeleton_placeholder" android:layout_marginBottom="16dp" />

        <!-- Target Goals Card -->
        <View android:layout_width="match_parent" android:layout_height="150dp" android:background="@drawable/skeleton_placeholder" android:layout_marginBottom="16dp" />

        <!-- Weekly Goals Card -->
        <View android:layout_width="match_parent" android:layout_height="300dp" android:background="@drawable/skeleton_placeholder" android:layout_marginBottom="16dp" />

    </LinearLayout>
</com.facebook.shimmer.ShimmerFrameLayout>
